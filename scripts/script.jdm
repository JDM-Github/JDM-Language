$include => Console;

jobject console = Console();
console.setAutoClear(jtrue);
console.setCreate(() => { jreturn jtrue; });

jdouble x = 0;
console.setUpdate(() =>
{
	console.drawBoxCycle(x, 0, 20, 20);
	x += 0.2;
	jreturn jtrue;
});

console.run("TEST", 100, 100, 10, 10);
